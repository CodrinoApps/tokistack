{
  "name": "@tokistack/authorizer",
  "version": "1.0.0",
  "description": "Tokistack API Gateway Lambda Authorizer",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.build.json --noEmit && esbuild src/index.ts --bundle --platform=node --target=node24 --outfile=dist/index.js --sourcemap",
    "test": "jest"
  },
  "license": "UNLICENSED",
  "dependencies": {
    "@tokistack/logger": "workspace:*"
  },
  "devDependencies": {
    "@tokistack/typescript-config": "workspace:*",
    "@types/aws-lambda": "8.10.149",
    "@types/jest": "30.0.0",
    "@types/node": "25.2.2",
    "esbuild": "0.27.3",
    "jest": "30.2.0",
    "ts-jest": "29.4.6",
    "typescript": "5.9.3"
  }
}
