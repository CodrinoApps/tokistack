{
  "name": "@tokistack/auth-handler",
  "version": "1.0.0",
  "description": "Tokistack Auth Lambda",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.build.json --noEmit && esbuild src/index.ts --bundle --platform=node --target=node24 --outfile=dist/index.js --sourcemap",
    "test": "vitest",
    "test:ci": "vitest run"
  },
  "license": "UNLICENSED",
  "dependencies": {
    "@tokistack/auth": "workspace:*",
    "@tokistack/db": "workspace:*",
    "@tokistack/logger": "workspace:*"
  },
  "devDependencies": {
    "@tokistack/test-utils": "workspace:*",
    "@tokistack/typescript-config": "workspace:*",
    "@types/aws-lambda": "8.10.161",
    "@types/node": "25.2.2",
    "esbuild": "0.28.0",
    "typescript": "5.9.3",
    "vitest": "4.1.2"
  }
}
